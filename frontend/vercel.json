{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "https://your-backend.example.com/api/$1"
    },
    {
      "src": "/uploads/(.*)",
      "dest": "https://your-backend.example.com/uploads/$1"
    }
  ]
}